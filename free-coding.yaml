$schema: https://learning-contents.org/schemas/free-coding/0.1.0
source: Program.cs
license: MIT
locale: en-US
id: free_coding_bank_account
title: Bank Account Management
description: Implement and test bank account methods in C#.
authors:
  - first: Jone
    last: Doe
    email: jone.doe@example.com
    affiliation: University of John Doe
pedagogy:
  activity: free-coding
  difficulty: novice
  prereq_topics: [classes]
  topics: [classes, methods, state]
runtime:
  language: csharp
statement: |
  Implement a `transfer` sequence or method in `Main` to move money from one `BankAccount` to another.
  Ensure that money is withdrawn from the current account only if funds are sufficient, and deposited into the target account.
executions:
  - title: Transfer Alice to Bob
    stdin: []
    stdout: |-
      Deposited 100
      Withdrew 50
      Balance: 250
    console: |-
      Deposited 100
      Withdrew 50
      Balance: 250
    status: pass
elements:
  - type: locked
    location:
      snippet: |-
        using System;

        public class BankAccount
        {
            public string Owner { get; set; }
            public int Balance { get; set; }

            public BankAccount(string owner, int balance)
            {
                Owner = owner;
                Balance = balance;
            }

            public int Deposit(int amount)
            {
                if (amount > 0)
                {
                    Balance += amount;
                    Console.WriteLine($"Deposited {amount}");
                }
                return Balance;
            }

            public int Withdraw(int amount)
            {
                if (0 < amount && amount <= Balance)
                {
                    Balance -= amount;
                    Console.WriteLine($"Withdrew {amount}");
                }
                else
                {
                    Console.WriteLine("Insufficient funds");
                }
                return Balance;
            }
        }
  - type: editable
    location:
      snippet: |-
        var account = new BankAccount("Bob", 200);
                account.Deposit(100);
                account.Withdraw(50);
                Console.WriteLine($"Balance: {account.Balance}");
    placeholder: |-
      // Implement transfer sequence here
              // Then test it with acc1 and acc2
testcases:
  - title: Simple Transfer
    visible: true
    stdin: []
    stdout: |-
      Deposited 100
      Withdrew 50
      Balance: 250
    console: |-
      Deposited 100
      Withdrew 50
      Balance: 250
